import { version } from '../../package.json';

export const environment = {
    msisApi: 'https://tqsgs1yg76.execute-api.us-east-1.amazonaws.com/prod/api',
    production: true,
    version: version
};
