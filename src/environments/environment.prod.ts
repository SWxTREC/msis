import { version } from '../../package.json';

export const environment = {
    // TODO: replace this with deployed vector API
    vectorApi: 'https://eqpbpvy4u0.execute-api.us-east-1.amazonaws.com',
    production: true,
    version: version
};
